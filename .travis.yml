language: node_js
node_js:
  - "7"
dist: trusty
branches:
  only:
    - staging

before_script:
  - nvm install 8.0
  - npm install
  - npm install -g firebase-tools

script:
  - npm run build


after_success:
  - firebase deploy --token=${FIREBASE_API_TOKEN} -P staging
